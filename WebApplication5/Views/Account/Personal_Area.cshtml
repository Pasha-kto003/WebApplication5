@model WebApplication5.Models.User
@{
    ViewBag.Title = "О пользователе";
    ViewBag.Title = Model.Email;

}
<h4>@ViewData["content"]</h4>
<h2>Пользователь @Model.Email</h2>
<div>
    <dl class="dl-horizontal">
        <dt>Почта</dt>
        <dd>@Model.Email</dd>
        <dt>Идентификатор</dt>
        <dd>@Model.Id</dd>
    </dl>
    <div class="form-group">
        <h5 class="h3 mb-3 font-weight-normal">Это ваш пароль</h5>
        <input type="password" id="password-input" value="@Model.Password" readonly class="form-control" placeholder="Введите пароль" asp-for="Password" />
        <span asp-validation-for="Password" />
        <label><input type="checkbox" class="password-checkbox" />Показать пароль</label>
    </div>
</div>
